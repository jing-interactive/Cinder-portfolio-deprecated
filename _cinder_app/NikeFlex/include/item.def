GROUP_DEF(helper)
ITEM_DEF(float, VIRTUAL_CIRCLE_RADIUS, 3.0f)
ITEM_DEF(bool,  IS_DEBUG_MODE, true)
ITEM_DEF(int,   OSC_PORT,       3333)

GROUP_DEF(interactive)
ITEM_DEF(float, EFFECTIVE_RADIUS, 30.0f)    // visitors will affect leds within radius of <EFFECTIVE_RADIUS> pixels
ITEM_DEF(float, MAX_POWER_RATIO, 0.5f)      // After <MAX_POWER_RATIO> of the leds are lit up, go to MAX mode
ITEM_DEF(float, IDLE_TO_INTERACTIVE_THRESH, 0.1f) // if sum of distance to scene is bigger than <IDLE_TO_INTERACTIVE_THRESH>, will go to interactive mode

GROUP_DEF(idle.general)
ITEM_DEF(float,	IDLE_STATE_SECONDS, 5.0f)
ITEM_DEF(float,	LED_LERP_FACTOR, 0.1f)
ITEM_DEF(float,	LED_SIN_FACTOR, 0.5f)
ITEM_DEF(float, IDLE_SWITCH_PERIOD, 5.0f)   // after <IDLE_SWITCH_PERIOD> seconds, idle animation will change

GROUP_DEF(idle.bullet)
ITEM_DEF(float, IDLE_BULLET_SPEED, 4.0f)    // move <IDLE_BULLET_SPEED> entry in one second
ITEM_DEF(float, IDLE_BULLET_FADEOUT_SPEED, 0.05f)

GROUP_DEF(idle.spark)
ITEM_DEF(float, IDLE_SPARK_INTERVAL, 0.5f)  // generate random sparks in every <IDLE_SPARK_INTERVAL> seconds
ITEM_DEF(int,   IDLE_SPARK_COUNT, 4)        // generate <IDLE_SPARK_COUNT> sparks every time